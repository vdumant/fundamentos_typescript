(() => {
  let productTitle = 'My product';
  productTitle = "This is 'My other title'";
  console.log(productTitle);

  const summary = `
  title: ${productTitle}
  title: ${productTitle}
  title: ${productTitle}
  `;
  console.log(summary);


})()
